# Grafana Dashboard Provisioning Configuration
apiVersion: 1

providers:
  - name: 'video-platform-dashboards'
    orgId: 1
    folder: 'Video Platform'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards